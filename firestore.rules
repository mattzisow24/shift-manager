rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Shared team document — any authenticated user can read/write
    match /teams/{teamId} {
      allow read, write: if request.auth != null;
    }
  }
}
